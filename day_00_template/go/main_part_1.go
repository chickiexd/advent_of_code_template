package main

import "fmt"

func main() {
    inputList, err := GetInputDataAsList(false)
    inputListTest, err := GetInputDataAsList(true)
    inputString, err := GetInputDataAsString(false)
    inputStringTest, err := GetInputDataAsString(true)
    
}
